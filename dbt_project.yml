name: 'robbins'
version: '1.0.0'
config-version: 2

profile: 'robbins'

model-paths: ["models"]
test-paths: ["tests"]
snapshot-paths: ["snapshots"]
clean-targets: ["target", "dbt_packages"]

models:
  robbins:
    staging:
      +materialized: view
    marts:
      +materialized: table
